const styles = theme => {
  return {
    container: {
      display: 'flex',
      flex: '1',
      justifyContent: 'flex-start',
      width: '100%',
      minHeight: '32px'
    }
  }
}

export default styles
