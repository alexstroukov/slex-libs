const styles = theme => {
  return {
    container: {
      width: theme.slexUi.iconButton.size,
      height: theme.slexUi.iconButton.size,
      minWidth: theme.slexUi.iconButton.size,
      minHeight: theme.slexUi.iconButton.size
    },
    icon: {
      fontSize: '17px'
    }
  }
}

export default styles
