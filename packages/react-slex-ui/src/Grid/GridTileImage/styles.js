const styles = theme => {
  return {
    container: {
      width: '100%',
      height: '100%'
    },
    image: {
      width: '100%',
      height: '100%',
      borderRadius: '2px',
      overflow: 'hidden'
    }
  }
}

export default styles
