const styles = theme => {
  return {
    container: {
      width: '100%',
      height: '100%',
      borderRadius: '2px',
      backgroundColor: '#939292'
    },
    dashed: {
      border: '1px dashed #f4f4f4',
      flex: 1,
      borderRadius: '2px'
    }
  }
}

export default styles
