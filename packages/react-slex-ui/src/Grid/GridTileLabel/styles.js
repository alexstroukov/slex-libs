const styles = theme => {
  return {
    container: {
      paddingTop: 2,
      display: 'flex',
      alignItems: 'center',
      flex: 1
    },
    label: {
      textOverflow: 'ellipsis',
      overflow: 'hidden'
    }
  }
}

export default styles
