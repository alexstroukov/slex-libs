const styles = theme => {
  return {
    container: {
      position: 'relative'
    },
    overlay: {
      top: 0,
      left: 0,
      position: 'absolute',
      width: '100%',
      height: '100%',
      cursor: 'default'
    },
    clickable: {
      cursor: 'pointer'
    }
  }
}

export default styles
