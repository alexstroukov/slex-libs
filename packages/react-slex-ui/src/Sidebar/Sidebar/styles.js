const styles = theme => {
  return {
    container: {
      display: 'flex',
      width: '100%',
      height: '100%',
      transition: 'background-color 0.3s ease-in-out'
    },
    menu: {
      paddingTop: 60,
      flex: 1
    }
  }
}

export default styles
