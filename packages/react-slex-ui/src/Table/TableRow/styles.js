const styles = theme => {
  return {
    container: {
      position: 'relative',
      width: '100%',
      display: 'flex',
      height: 'auto',
      minHeight: '48px'
    }
  }
}

export default styles
